#!/usr/bin/env python3

"""Converts Markdown to HTML. Usage:

  md2html [pathnames...]"""

import markdown
import sys

if len(sys.argv) == 1:
  markdown.markdownFromFile()
else:
  for pathname in sys.argv[1:]:
    markdown.markdownFromFile(input=pathname)
