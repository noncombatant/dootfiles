#!/usr/bin/env bash

source "$HOME"/bin/script.sh

cleanup() {
  rm ./temp.*
}

for file in $@; do
  declare tmp=$(mktemp ./temp.XXXXXX)
  clang-format -style=Chromium "$file" > "$tmp"
  mv "$tmp" "$file"
  chmod 644 "$file"
done
