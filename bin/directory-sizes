#!/usr/bin/env bash

source "$HOME"/bin/script.sh

help="Shows a list of the largest directories. Usage:

  directory-sizes pathname [...]"

test $# -gt 0 || usage
height=$(( $(tput lines) - 2 ))
du -k "$@" | sort -bn | tail -n $height
