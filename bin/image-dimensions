#!/usr/bin/env bash

help="Prints out the width and height of images. Usage:

  image-dimensions image [...]"

source "$(dirname "$0")/script.sh"

sips --getProperty pixelWidth --getProperty pixelHeight "$@"
